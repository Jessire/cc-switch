# CC Switch 项目 Agent 规则

## 文档用途

- 本文件写给在此项目中工作的 Agent, 不是写给用户的使用说明.
- 将 Jessire 在本项目历史对话中已经确认的需求, 偏好, 禁区和验收方式作为默认约束.
- 不得用上游仓库的通用做法覆盖本文件中的个人定制决策.
- 用户当轮明确要求高于本文件; 出现冲突时按用户最新要求执行.

## 项目定位

- 将此仓库视为 Jessire 日常使用的 CC Switch Windows 定制版.
- 主要目标是管理大量中转供应商, Codex Desktop 自定义模型, 分组, 切换和重启行为.
- 默认只交付 Windows x64. 未经明确要求, 不为 macOS 或 Linux 增加工作量, 构建或发布产物.
- 保留上游功能和后续同步能力, 但不得为了同步方便删除或弱化个人定制功能.
- 不主动同步上游. 用户要求同步时, 合并最新 `origin/main` 到定制 `main`, 逐项解决冲突并保留双方有效功能.

## Git 主线

- 只使用一个长期分支 `main`, 不自行创建功能分支, worktree 或 PR.
- `origin` 代表上游 `farion1231/cc-switch`; `fork` 代表 Jessire 的仓库; `main` 跟踪 `fork/main`.
- 同步上游使用 `fetch origin -> merge origin/main -> verify -> push fork main`.
- 禁止用 reset, 强制推送, 整文件选边或重建分支来处理上游同步.
- 源码改动完成相关测试并通过本地 Windows Release 编译后, 自动提交到 `main`, 不再等待用户提醒.
- 本地编译必须先于提交; GitHub Actions 构建不能替代提交前的本地编译.
- 仅文档, 规则或工作流改动不要求无关的 EXE 编译; 完成对应格式和语义校验后自动提交.
- 自动提交不等于自动推送. 未经用户明确要求, 不推送, 不打标签, 不发布 Release.
- 用户说“提交上去”时, 完成验证和本地编译后直接提交并推送 `main`; 提交信息保持简洁, 不含 AI 署名.

## 用户数据与运行状态

- 替换或删除旧程序时, 只处理明确指定的 EXE 或构建产物. 保留 CC Switch 数据库, 设置, 备份和各客户端真实配置.
- 禁止因重新构建或覆盖 EXE 而结束当前正在运行的 CC Switch 或 Codex Desktop, 以免中断正在进行的对话.
- 目标 EXE 被占用时, 先生成旁路文件, 例如 `CC-Switch-Custom-New.exe`; 不得强制结束进程.
- 只有用户明确要求切换到新版本时, 才关闭旧实例并完成替换.
- API Key, token, cookie, OAuth 数据和真实配置内容必须脱敏; 不得写入源码, 测试夹具, AGENTS.md, 日志或提交.

## 供应商分组

- 支持将大量供应商批量加入分组, 也支持单个供应商通过卡片右侧图标加入现有分组.
- 一个供应商必须能同时属于多个分组, 因为同一中转可能提供多类模型.
- 在供应商名称旁显示它所属的分组名称.
- 分组标签支持拖动排序; `全部` 和 `未分组` 同样允许移动, 不固定在最前面.
- `添加分组` 永远放在分组列表最后.
- 分组标签的整个可见区域都必须可点击, 不得只有文字中央区域响应.
- 右键或单选操作不得被批量选择机制阻断.
- 修改分组逻辑时, 同时验证创建, 重命名, 删除, 排序, 单个加入, 批量加入, 多分组归属和筛选结果.

## 切换与重启语义

- “保存配置”只表示保存编辑内容, 不表示启用该供应商, 不得附带重启客户端.
- 不提供“保存并重启”作为编辑页主流程.
- 顶部循环箭头是可点击的手动重启按钮, 用于立即重启当前选中的客户端.
- 自动重启开关只控制“成功切换供应商后是否重启对应客户端”.
- 同组不重启开关按当前供应商和目标供应商是否共享任一自建分组判断, 不依赖当前打开的分组标签.
- 同组切换且启用跳过规则时不得重启; 跨组切换且启用自动重启时才重启.
- 重启进程必须静默, 不弹出 CMD 或 PowerShell 窗口.
- 重启实现必须针对当前客户端: Codex 切换重启 Codex Desktop, 其他客户端切换重启各自对应程序.
- 修改重启功能后, 必须分别验证手动重启, 自动重启, 关闭自动重启, 同组跳过和跨组重启.

## Codex Desktop 模型

- 验收目标是 Codex Desktop, 不得只实现 Claude Code 或 Codex CLI 可用.
- 自定义模型应追加到 Codex Desktop 模型菜单并与内置模型共存, 不得把菜单退化成只剩一个笼统的“自定义”入口.
- 新加入菜单的模型默认排在前面, 并支持拖动排序.
- Codex 不保留独立的“默认模型”输入区; 菜单顺序第一项就是默认模型.
- 保存时, 菜单非空则将第一项模型 ID 写入 Codex 顶层 `model`; 菜单为空时保留原值, 不主动清空.
- Grok Build 等复用组件若仍需要独立默认模型, 必须保留其原有行为, 不得因简化 Codex 一并删除.
- 可用模型采用紧凑布局, 桌面宽度下每行最多显示 3 个; 上下文窗口与对应模型放在同一行.
- 修改模型功能时, 验证模型获取, 勾选, 排序, 显示名, 模型 ID, 上下文, 保存结果和 Codex Desktop 实际菜单.

## 通用配置与回写

- 新增统一供应商生成的各应用供应商默认启用通用配置.
- 现有供应商缺少通用配置时, 按用户要求批量补齐; 用户显式关闭的供应商必须保留关闭状态.
- 更改 Grok MCP, Codex provider 或其他由 CC Switch 管理的配置时, 必须修改 CC Switch 的持久化源, 不能只改最终 live 文件.
- CC Switch 重启后会重新写入受管配置. 验证必须包含“修改 -> 重启 CC Switch -> 再次读取配置”, 防止设置恢复旧值.
- 测试上游 API 时, 实际调用目标功能; `/models` 成功不代表聊天, 搜索或 Responses 链路可用.
- 区分请求被本地规则拒绝, 上游错误, 超时和 fallback 成功, 不得把它们合并成“接口不可用”.

## 主界面与编辑页

- 主界面优先密集, 清楚和便于高频操作, 避免大块空白和重复说明.
- 供应商编辑页顶部保持紧凑: 小图标放在供应商名称前; 名称, 备注, 官网链接同一行; API Key 和 API 地址同一行.
- 对齐输入框和标签高度; 完整 URL 文案可以缩小, 但不得挤压 API 地址输入区.
- 删除无实际帮助的黄色提示和重复入口; 不使用常驻说明占用模型区空间.
- 用户已有选中状态和颜色反馈时, 不重复实现同类视觉状态.
- 修改布局后必须用实际 Windows GUI 检查, 不能只依赖组件测试或静态代码判断.

## 托盘, 启动和网页导入

- 正常启动应显示主界面, 不得只静默最小化到托盘或出现白屏.
- 托盘左键双击打开主界面; 取消左键单击打开行为; 右键单击保留菜单.
- 网页通过官方 deep link 导出到 CC Switch 的能力不得被定制版破坏. 官方版本能导入的链接, 定制版也应能导入.
- 验证托盘或 deep link 时必须运行实际 Release EXE, 检查窗口, 单实例和真实导入结果.

## 调研规则

- 用户要求调研同类软件时, 优先检索真正相关的本地桌面供应商或模型管理工具, 不混入服务器面板和泛 AI 项目.
- 相关性优先于 Star 数; 满足相关性后优先选择至少 1K Star, 且最近一周仍有更新的项目.
- 用户要求全面调研时至少检查 10 个有效候选, 包含其仓库, 功能实现和近期活跃证据.
- 不只列产品名称. 必须说明哪些功能能直接用于 CC Switch, 实现方式和是否适合当前 Windows 定制版.
- 涉及“不同窗口使用不同模型”时, 重点验证 Codex Desktop 是否可实现, 不得用 Claude Code 的方案代替.

## 构建与交付

- 交付可双击运行的 Windows Release GUI EXE, 不把 debug 目录, 开发终端或安装命令当作最终产物.
- 本机构建使用:

```powershell
pnpm tauri build --no-bundle
```

- 原始产物位于 `src-tauri/target/release/cc-switch.exe`.
- 桌面交付文件默认命名为 `CC-Switch-Custom.exe`; 目标正在运行时使用 `-New`, `-New2` 等旁路名称, 不结束旧实例.
- GitHub 构建使用 `Build Windows EXE` 手动工作流, 产物固定为 Windows x64 Artifact `CC-Switch-Custom-Windows-x64`.
- 长时间 Rust Release 构建必须持续跟踪到明确成功或失败. 不得因为工具超时提前结束任务, 也不得重复启动多个 Cargo 构建争用同一 target 目录.
- 构建完成后报告可点击的绝对路径, 文件名, 版本, x64 架构, 大小和 SHA256, 并明确说明当前运行的是哪个版本.

## 验证门槛

- 先运行与改动直接相关的测试, 再按影响范围运行 TypeScript, 前端测试, renderer 构建和 Rust 测试.
- Windows 功能以 Windows 实测为准. 用户只要求 Windows 时, 不把 macOS 或 Linux 结果作为交付阻塞项.
- UI 改动至少验证实际 Release 窗口, 点击区域, 排版和对应操作结果.
- 配置改动至少验证数据库或持久化源, 生成的 live 配置, CC Switch 重启后的稳定性和目标客户端实际效果.
- 自动重启或模型菜单变更不得通过结束当前 Codex Desktop 来测试; 使用隔离实例, 测试钩子或等待用户允许.
- 不得以“应该可以”“编译通过”或 HTTP 200 代替完成. 只有实际验证通过, 或明确指出真实阻塞, 才能收口.

## 汇报要求

- 用户问“软件在哪”或要求构建后, 第一时间给出最终 EXE 的可点击绝对路径和直接打开方式.
- 只汇报最终行为, 实际验证结果, 产物位置和未验证边界; 不输出冗长尝试过程.
- 不让用户反复发送“继续”. 已开始的构建, 测试, GitHub Action 或修复必须跟踪到终态再结束.
- 用户说“不急提交”时只保留本地改动, 明确报告未提交状态.
